<h1>Reminders</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task[:completed] ? '☑' : '□' %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<%= link_to '+ New errand', tasks_new_path %>
